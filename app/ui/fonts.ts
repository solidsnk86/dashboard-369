import { Inter, Lusitana, Press_Start_2P } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });

export const lusitana = Lusitana({
  weight: ['400', '700'],
  subsets: ['latin'],
});

export const ps2 = Press_Start_2P({
  weight: ['400'],
  subsets: ['latin'],
});
